# Known issues

Every entry below was verified against the working tree, not written from
memory. If you fix one, delete the row — a stale issues list is worse than none.

**Severity means:** *High* — contradicts a stated project rule, or ships a claim
we cannot support. *Medium* — real defect, contained, no reader is misled.
*Low* — friction. *Info* — worth knowing, not worth fixing yet.

| # | Sev | Issue | Where | Notes |
|---|---|---|---|---|
| ~~0~~ | ~~High~~ | ~~**Module 1 never introduced the two kinds of charge**, and stated that an electron "carries e = 1.602…×10⁻¹⁹ C" — wrong by a sign. `positive`/`negative` appeared zero times across all three scenes and the page text.~~ **FIXED.** | `Scene1Counting.tsx`, `physics/current/model.ts`, `model.yaml`, MDX | Found by review, not by the test suite — a reminder that tests check the code against the declared model, and cannot notice that the model itself was silent about something. Sign is now in Scene 1 only; Scenes 2–3 stay deliberately unsigned, recorded as an `external_coupling`. |
| **0c** | High | **Module 1 Scene 1 names particles the students have never met.** Its legend reads *"proton — charge +e"* / *"electron — charge −e"* and the steppers are labelled with particle names. The course author has now confirmed the entry assumption: **students have heard the word *atom* but do not know what is inside one.** | `Scene1Counting.tsx` | The scene asserts protons and electrons with no evidence in the module that either exists — and the balloon experiment that motivates the two kinds shows nothing about what carries them. Fix: relabel as **kind + / kind −** or **positive / negative units**; defer particles to the later node where evidence exists. See `LESSON_E1_what-is-charge.md` §9, §18. |
| **0b** | High | **The elementary charge is declared twice**, in two physics modules, with no shared import. `src/physics/current/model.ts:33` and `src/physics/current-factors/model.ts:26` each define `E = 1.602176634e-19`. Identical today by luck. | both files | Correct one and the other survives; each module's tests verify against its own copy, so **the suite cannot detect the divergence**. This is the exact common-mode failure the verification design exists to prevent, in our own source. Fix: one `src/physics/constants.ts`, imported by both. Charter §7.2 — a model is never duplicated to serve a second question. |
| **1** | High | **Closed-form formulas live in the visualization layer**, violating `AGENTS.md` rule 1 ("if you write a formula in a `.tsx` file, it is in the wrong place"). Two occurrences. | `src/components/physics/ProjectileSimulation.tsx` | Fix: extract to `src/physics/projectile/analytical.ts` and import **into the UI only**. Tests must keep their own inline restatement — if UI and tests import one module, an error there shows up as agreement in both. |
| **2** | High | **Three of the four unsourced values are still unsourced** and are rendered to readers. | `src/content/electricity/what-is-current/model.yaml` (3), `.../what-determines-current/model.yaml` (1) | They are correctly labelled ⚠ and confined to illustrative panels, so nothing in a scene depends on them. Still blocks pointing students at the site. See `BACKLOG.md` §1. |
| **3** | Medium | **Undocumented approximation.** The equipotential map calls `potential(..., softening = 0.02)`; `softening` appears **zero times** in the module's `model.yaml`. Violates `AGENTS.md` rule 6 (no approximation without recording it). | `src/components/physics/ElectricFieldSimulation.tsx` | Add to `representation_choices` and `implementation.notes`. |
| **4** | Medium | **`g = 9.81` is typed as a plain parameter default** when it is a `conventional` value — latitude- and altitude-dependent, not a constant of nature. The module declares the uniform-field *assumption* but never says the *number* is a convention. | `src/content/mechanics/projectile-motion/model.yaml` | Blocked deliberately: do not touch until the quantity taxonomy is applied across all four modules at once. See `docs/specs/charge-and-current.md` §8. |
| **5** | Medium | **`traceFieldLine` has zero test coverage** — and it is the one physics-layer function that is arguably visualization logic, so it sits exactly on the boundary the architecture rests on. | `src/physics/electric-field/model.ts` | Testable properties: lines terminate on charges or leave the domain, never cross, and reverse direction under charge sign flip. |
| ~~6~~ | ~~Medium~~ | ~~Mutation testing exists only as prose.~~ **FIXED.** `scripts/mutate.sh` is committed and runs: 15 mutants, 15 caught, 0 survived. The README's claim is now checkable rather than trusted. | `scripts/mutate.sh` | Run with `bash scripts/mutate.sh`. Exits non-zero if any mutant survives — a survivor is a gap in the suite, and the response is to write the missing test, not to delete the mutation. |
| **7** | Low | **Licence field disagrees with `CONTRIBUTING.md`.** `package.json` says `"CC-BY-4.0"`; `CONTRIBUTING.md` says code MIT, content CC BY 4.0. | `package.json` | Use an SPDX expression, e.g. `"MIT AND CC-BY-4.0"`. |
| **8** | Low | **Astro content-collection warning on every build.** `src/content/*` holds `model.yaml` files and no `.md`/`.mdx`, so Astro auto-generates empty collections and warns. | build output | Add `src/content.config.ts` defining the collections explicitly, or move the YAML outside Astro's content root. |
| **9** | Low | **8 npm advisories** (1 critical, 3 high), all transitive through `sharp`/libvips. | `package-lock.json` | Build-time image tooling only; no runtime exposure on a static site. `npm audit fix --force` wants Astro 7 — do it deliberately, not reflexively. |
| ~~10~~ | ~~Low~~ | ~~A stale `.git/index.lock` blocks committing.~~ **FIXED** 11 Aug. Both `.git/index.lock` and `.git/HEAD.lock` were timestamped `2026-08-09 10:51` — the same minute as the only commit, so a git process died mid-commit and every operation for two days failed silently. All 35 files committed as `217b50d`. | `.git/` | Severity was wrong. Filed Low; it was the reason Modules 1 and 2, both test files, `mutate.sh`, this file and `docs/specs/` existed on one machine with no commit and no remote. A lock file that blocks all recording is High. |
| **11** | **High** | **E1 is implemented twice, in two stacks.** `E1Investigation.tsx` (903 lines, here) and `e1.js` (845 lines, static tree). Same question, same lesson, two independent implementations. A correction to E1 currently has to be made twice or the two silently diverge. | `src/components/investigate/E1Investigation.tsx` · `../openvidya-repo/e1.js` | Resolves when E2 is ported and the static tree is retired. Until then, **edit neither without editing the other**. See `docs/D1_REPOSITORY_DECISION.md` §4.1. |
| **12** | **High** | **E2's apparatus has never been built** (blocker B3). Whether the pair settles faster than it leaks is pass/fail for the entire lesson, and E2 is deployed to students' reach. No amount of review settles it. | `docs/LESSON_E2_AMENDMENTS.md` B3 | Pass criterion must be **derived and written down before the first measurement** — settling short enough that charge lost during it is small against the precision with which r can be read. Not chosen after seeing the data. |
| **Info** | — | The two galvanometer-style needle widgets in the sibling `byoPhysics` bench and OpenVidya's simulations solve overlapping problems with no shared code. Not a defect; a signal that the two projects need a reconciliation decision. | — | See `BACKLOG.md` §6. |

## Verified state at time of writing

- **86 tests pass** across 4 files (projectile 28, electric-field 20, current 28, current-factors 10).
  *Corrected 11 Aug: this line said 78, and `current` 20. Re-counted by running the
  suite on a fresh install, not by reading it. A stale number in the file whose
  header promises everything was "verified against the working tree" is the exact
  failure the file exists to prevent.*
- **Mutation run: 15 injected faults, 15 caught, 0 survived.**
- Site builds: 5 pages, no errors.
- Module 2 (`what-determines-current`) shipped **with** a test file and a `model.yaml` — the methodology held across a change of author, which is the first real evidence it transfers.
